from 10 ios there are property of all scroll view - refresh control

For hashed containers, such as Dictionary and Set, there’s a new type AnyHashable that can hold a value of any type conforming to the Swift Hashable protocol

The elements that are casted to AnyObject in the array are now class types of Objective-C: NSString and NSNumber

Interesting but for header and footer NO automatic height. You must specify in table properties or in delegate method, using UITableViewAutomaticDemensions brings you to zero

To animate change height of table view cell use begin/end updates
class we can override but static is not

Effective knowledge workers, in my observation, do not start with their tasks. They start with their time

We can create observables for combining. These can help us a lot

// 04-12-17
1. You love other person or not. That’s all
2. Become leader whenever you can
3. Act. Do not think

// 04-13-17

1. We have got mirror. And we can receive children and self type, with naming of structure / class. We can reflect all properties of mirror. We can override obit if we want to. So for convenient just use Mirror(reflecting: self)
2. Revert table view. Just use affineTransform for .pi
3. Do whatever you want. And do not stop. Ever

// 05-14-17

1. We can use drive(on_observer) to pass data
2. Drive is not silver bullet. Sometimes better manage st
3. People are the most important thing uff manually

// 04-15-17

1. Try to think in reactive way
2. Socialization is not so hard
3. Promise less - perform more

// 04-16-17
1. No one cares

// 04-17-17
1. Xcode server bunched with Xcode
2. Jenkins with fast lane is most powerful thing.

// 04-18-17
1. automaticallyAdjustScrollViewInsets. Work only for first view(recursive)

// 04-19-17
1. English is so cool
2. Set for label adjust and lineBreakMode for shrinking

// 04-20-17
1. Do not show keyboard in willAppear
2. Write as clean as possible
3. About silver bullet code

// 04-21-17
1. UIImage has got highlight image property
2. Use system layout size fitting to determine size of things that using auto layout
3. Self in extension

// 04-22-17
1. triple = for compare within types in swift
2. new rx operators:
    1. deferred
    2. never
    3. range(start, count)
    4. repeatElement
    5. timer
    6. amb
    7. concat
    8. switchLatest
    9. withLatestFrom
    10. zip
    11. ignoreElements
    12. sample
    13. single (withPredicate)
    14. throttle
    15. buffer
    16. flatMap (sequence -> observable)
    17. window
    18. reduce
    19. scan
    20. catch / catchError
    21. multicast
    22. publish
    23. refCount
    24. debug
    25. delaySubscription

// 04-25-17
1. NSUserDefaults set nil is the same as using `removeObjectFor(key)`
2. synchronize() called meanwhile. Better to force it call sometimes

// 04-27-17
1. Hijacking vs hitchhiking

// 04-28-17
1. pay attention to work without net, background update and error handling.
2. interesting solution about keyboard
3. never send to review until SELF REVIEW COMPLETED

// 04-29-17
	1. Using unicode in table view decrease performance

// 04-30-17
	1. Observe methodInvoked from forward delegates
	2. Button got minimal intrinsic size of 30 points. That means to set width to it, you should reference to width directly